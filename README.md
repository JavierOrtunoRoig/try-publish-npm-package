# Try of create a npm library

Probando con un proyecto hermano se puede usar la librería con:

```javascript
import { sum, subtract } from '../../try-publish-npm-package/dist/my-lib.js'

console.log(sum(1, 2));
console.log(subtract(1, 2));
```
